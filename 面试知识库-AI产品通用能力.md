# AI 产品通用能力

## 请做一下自我介绍（为什么转向 AI 产品 / 你的核心优势是什么）

**关键词：** 5 年产品经验、AI 产品经理、UI/UX、ToB、项目交付
**适用场景：** 开场自我介绍、经历概览、岗位匹配度追问
**回答：** 您好，我叫徐浩，有 5 年产品经验，目前在阿里云 MaaS 做 AI 产品经理，主要负责大模型应用、RAG、Agent 和企业级 AI 平台建设。我早期做过 UI/UX，之后在阿里企业智做售前产品，累计落地 10 多个大型项目；后来负责过酒店、公寓和文旅 SaaS，也参与过智能客服从 0 到 1 建设。我的优势是既能理解用户与业务，也能把 AI 能力拆成数据、知识、模型、工作流、评测和上线治理，并推动跨团队落地。

**背景：** 我的经历从体验设计、ToB 售前和 SaaS 产品逐步延伸到生成式 AI 产品，能力重点也从功能设计转向端到端 AI 系统设计。
**具体做法：**
1. 先从用户问题和业务指标判断是否值得使用 AI，而不是为了使用模型而设计功能。
2. 再把方案拆到数据、RAG、Agent、工具、权限、评测和人工兜底，与算法和工程团队对齐边界。
3. 上线前定义验收集、异常流程和监控口径，上线后根据 Bad Case 和业务反馈持续迭代。

**结果：** 源材料可确认的是 5 年产品经验、10 多个大型项目交付经历，以及目前在阿里云 MaaS 从事 AI 产品工作；具体项目阶段和业务结果分别在项目知识库中说明，不在通用介绍里扩大表述。

**追问补充：**
- 如果追问个人职责，我会按“我决策了什么、我设计了什么、团队实现了什么”区分边界。
- 如果追问代表项目，我会选择与岗位最相关的一项展开，不在自我介绍里堆叠项目细节。

## AI 产品经理具体做什么（与普通产品经理有什么区别）

**关键词：** 业务判断、技术方案、评测、上线治理、端到端负责
**适用场景：** 岗位认知、能力模型、职业动机追问
**回答：** 我认为两类产品经理都要对用户价值和业务结果负责，区别在于 AI 产品经理还要管理模型的不确定性。除了需求、流程和体验，我还需要参与数据准备、模型与 RAG/Agent 方案选择、Prompt 或 Workflow 设计、评测集建设、成本与时延治理、线上监控和持续迭代。

**背景：** AI 功能不是规则写完就稳定输出。同一个输入可能受知识、上下文、模型版本和工具状态影响，因此产品设计必须同时覆盖效果、成本、速度、风险和人工协作。
**具体做法：**
1. 定义业务问题、目标用户和成功指标，判断规则、搜索、传统算法或大模型哪种方案更合适。
2. 画清数据层、知识层、模型/Agent 层和应用层，明确每层输入输出、权限和责任人。
3. 设计正常流、异常流、低置信度表达、引用、审核和人工接管。
4. 用离线评测、灰度、线上监控和用户反馈验证质量、成本、时延与业务价值。

**结果：** 这套职责的验收不是“接入了大模型”，而是产品能在明确边界内稳定解决问题，并能解释效果、复现问题和持续优化；具体指标要根据业务场景设定。

**追问补充：**
- AI 产品经理不一定亲自训练模型，但要能说明为什么这样选型、怎样验收以及失败时怎么办。
- 如果 AI 相比确定性方案没有明显价值，我会优先选择更简单、可控的方案。

## 什么是 RAG（什么时候应该或不应该使用 RAG）

**关键词：** 检索增强生成、外部知识、混合召回、Rerank、引用与兜底
**适用场景：** RAG 概念、知识库方案、技术选型追问
**回答：** RAG 是先从外部知识源检索相关证据，再让大模型基于证据生成答案。典型链路是问题理解、权限过滤、召回、排序、上下文组装、模型生成和引用输出。我会在知识经常更新、答案需要私域数据或需要可追溯证据时使用 RAG；如果任务只依赖固定规则、简单结构化查询，或权威数据必须实时精确返回，我会优先使用规则、数据库或业务 API。

**背景：** 大模型参数中的知识可能过时，也不了解企业私域内容；但 RAG 不能自动保证正确，切片、召回、排序、权限和生成任何一环都可能引入错误。
**具体做法：**
1. 先治理知识来源、版本、权限、有效期和元数据，再按标题、段落与语义边界切片。
2. 用关键词检索覆盖名称、数字和缩写，用向量检索覆盖同义表达，再通过融合与 Rerank 筛选证据。
3. 将事实证据、参考范例和用户上下文分区，要求回答带引用；证据不足时明确说不知道或转权威接口/人工。
4. 分别评测召回命中、排序质量、引用一致性、答案正确性、时延和成本，并沉淀 Bad Case 回归集。

**结果：** 通用验收目标是答案能够回到权限正确、版本有效的原始证据，并能定位错误发生在检索还是生成环节；没有具体评测集和口径时，我不会承诺准确率数字。

**追问补充：**
- Embedding 和向量库解决语义召回，BM25 擅长精确词，Rerank 负责对候选证据再排序。
- RAG 负责找证据，不替代业务系统中的实时价格、库存、权限校验和确定性计算。

## 什么是 Agent（什么时候用 Agent、Workflow 或 Tool）

**关键词：** 目标驱动、规划、工具调用、状态、工作流、人工接管
**适用场景：** Agent 架构、技术选型、多 Agent 追问
**回答：** 我把 Agent 理解为围绕目标感知上下文、规划下一步、调用工具并根据结果继续决策的系统。需要多轮理解、动态规划或根据中间结果改变路径时，我会考虑 Agent；步骤固定、规则明确的流程优先用 Workflow；查询、计算、写入和发送等确定性动作交给 Tool。是否使用多 Agent，取决于任务是否需要隔离上下文、权限和评测，而不是 Agent 数量越多越好。

**背景：** Agent 提高了复杂任务的自主性，也增加了时延、Token、状态管理、越权和失败点。很多场景用单 Agent 加少量工具，甚至直接 Workflow，会更稳定。
**具体做法：**
1. 先拆任务图，标明节点输入输出、依赖关系、可并行项和终止条件。
2. 对确定性节点直接调用工具；只把需要理解、判断和规划的节点交给模型。
3. 用结构化 Schema 传递状态和证据，设置超时、重试、幂等、权限与预算上限。
4. 高风险外部动作增加确认，低置信度或连续失败时降级到规则、人工或安全回复。
5. 对每个节点和端到端任务分别做 Trace、评测和 Bad Case 归因。

**结果：** 通用验收标准是任务完成率、关键步骤正确率、工具调用成功率、时延、成本和人工接管率达到场景要求；真实数值必须通过具体项目评测确认。

**追问补充：**
- Memory 保存必要的历史与状态，Planning 决定下一步，Tool 执行动作，Workflow 约束确定路径。
- 多 Agent 适合职责和权限边界明显的复合任务；简单任务优先走低成本直达路径。

## 什么是 MCP（它和 Function Calling / Tool Calling 有什么关系）

**关键词：** Model Context Protocol、标准接口、Client、Server、工具与资源
**适用场景：** MCP 概念、工具生态、系统集成追问
**回答：** MCP 是连接 AI 应用与外部能力的开放协议。MCP Server 按统一方式暴露工具、资源或提示模板，MCP Client 负责发现和调用这些能力。Function Calling 或 Tool Calling 更接近模型“决定调用哪个函数以及传什么参数”的机制，MCP 解决的是外部能力如何被标准化接入和复用，两者可以配合使用。

**背景：** 如果每个模型应用都为数据库、文档、搜索或业务系统单独开发适配层，接入和维护成本会很高；MCP 提供统一协议，但不会自动解决业务权限和结果正确性。
**具体做法：**
1. 先定义 Server 暴露的能力、参数 Schema、返回结构和错误语义。
2. 在 Client 侧控制服务发现、连接、超时、重试和上下文注入，不把全部能力一次性塞给模型。
3. 在业务层补齐身份认证、最小权限、敏感参数校验、高风险确认和审计日志。
4. 对调用成功率、参数正确率、返回质量、时延和降级链路做独立评测。

**结果：** MCP 的预期价值是降低多应用、多工具之间的重复适配成本；是否真正降本以及能否稳定运行，需要结合接入数量、维护成本和调用指标验证，不能只凭“接入 MCP”下结论。

**追问补充：**
- MCP 不是 Agent，也不等于模型能力；它提供连接标准，Agent 仍负责判断和编排。
- MCP 统一接口后仍要做鉴权、授权、审计、限流和高风险动作确认。

## 什么是 Skill（什么时候用 Skill，什么时候用 MCP）

**关键词：** 可复用能力、任务说明、工作流封装、能力边界、协议接入
**适用场景：** Skill 概念、Agent 能力设计、MCP 与 Skill 选型追问
**回答：** 我把 Skill 定义为面向某类任务的可复用能力单元，它可以封装任务说明、步骤、约束、示例以及需要调用的工具。Skill 解决的是“这类任务应该怎样完成”，MCP 解决的是“外部工具和资源怎样用统一协议接入”。一个 Skill 可以通过 MCP 调用外部系统，也可以直接编排本地工具或固定流程，两者不是替代关系。

**背景：** 如果把所有规则都写进一个超长 Prompt，能力难以发现、复用、测试和版本管理；如果只接入 MCP 而没有任务方法，模型也未必知道怎样把工具组合成可靠结果。
**具体做法：**
1. 按稳定的业务目标划分 Skill，明确触发条件、输入输出、适用边界和失败处理。
2. 将确定性步骤、业务规则、示例和质量门槛写进 Skill，把实际查询或写入交给工具。
3. 只有当前任务命中时再加载相应 Skill 和最小工具集合，控制上下文与权限范围。
4. 对 Skill 做版本管理，并用标准用例、历史 Bad Case 和端到端任务回归。

**结果：** Skill 的通用验收目标是同类任务可以稳定复用、独立测试并清楚定位失败；收益需要通过任务成功率、维护成本、上下文消耗和复用次数验证。

**追问补充：**
- 简单、固定的单步动作不必包装成复杂 Skill，直接工具调用更清楚。
- Skill 是能力与方法边界，Tool 是确定性执行接口，MCP 是一种标准化接入方式。

## 如何搭建 AI 产品评测体系（模型、RAG 和 Agent 分别怎么评）

**关键词：** 黄金集、离线评测、在线指标、分层归因、Bad Case、回归测试
**适用场景：** 评测体系、上线验收、效果优化追问
**回答：** 我会把评测分成三层：结果层判断用户任务是否完成，链路层定位检索、模型、工具或编排哪里出错，业务层验证采用、转化或效率是否改善。模型看事实与指令遵循，RAG 额外看召回、排序和引用一致性，Agent 还要看规划、工具参数、状态流转、终止条件和端到端成功率。

**背景：** 只看一个“准确率”无法解释系统质量；模型升级后即使平均分提高，也可能让关键场景退化，因此评测必须有场景分层、版本和回归机制。
**具体做法：**
1. 从核心任务、边界条件、线上日志和人工反馈构建黄金集，并把高风险样本与历史 Bad Case 单独分层。
2. 为每类样本定义输入、标准答案或 Rubric、必要证据、自动指标和人工抽检规则。
3. 固定数据、模型、Prompt、知识库、工具和评分版本，先做离线基线，再灰度和 A/B 验证。
4. 将失败归因到输入、检索、生成、工具、编排、权限或产品流程，修复后回灌回归集。
5. 同时监控质量、时延、Token、成本、错误率、接管率和业务指标，避免单指标优化。

**结果：** 评测体系的产出应包括可复现基线、分场景结果、失败归因和上线门禁；阈值、样本量和业务目标必须由具体项目校准，信息不足时明确标为待确认。

**追问补充：**
- LLM-as-a-Judge 适合规模化初筛，但要用人工一致性抽检和规则指标校准。
- 线上用户反馈不能直接等同于模型质量，需要结合任务难度、流量结构和产品流程分析。

## 如何兼顾 AI 产品的成本、时延和稳定性（线上不稳定怎么办）

**关键词：** 模型路由、Token 治理、缓存、超时重试、降级、可观测性
**适用场景：** 工程化落地、MaaS、线上治理、成本追问
**回答：** 我不会把质量、成本和稳定性割裂处理，而是先按任务价值和风险分级，再给不同任务配置模型、上下文、预算和降级策略。简单任务走小模型、规则或直接工具，复杂任务才使用强模型和 Agent；线上通过限流、超时、重试、熔断、降级、人工接管和全链路 Trace 控制故障影响。

**背景：** AI 链路可能同时依赖模型、向量库、工具和外部服务。盲目加长上下文、串行调用多个 Agent 或无限重试，会让成本和时延快速上升，也可能放大错误。
**具体做法：**
1. 建立任务分级与模型路由，限制最大 Token、工具次数、Agent 步数和总执行时间。
2. 通过检索裁剪、上下文摘要、缓存、批处理和可并行节点降低重复消耗与等待。
3. 为每个外部依赖设置超时、有限重试、幂等键、熔断和备用模型/接口，避免级联故障。
4. 对高风险或证据不足场景返回可解释状态，并允许重试、修改输入或转人工，不伪造成功。
5. 按租户、场景、模型和链路监控调用量、Token、单位任务成本、P95 时延、错误率与降级率。

**结果：** 通用目标是在业务可接受的质量门槛下优化单位成功任务成本和端到端时延，并让故障可发现、可隔离、可恢复；源材料没有单位调用成本或线上稳定性数据，因此具体数值待项目实测。

**追问补充：**
- 重试只适合瞬时错误；参数错误、权限错误和确定性失败应立即终止并返回明确原因。
- 成本要看“每个成功任务”的总成本，而不是只看单次模型调用价格。

## 如何复盘一个 AI 项目（怎样讲清业务、技术和个人价值）

**关键词：** 业务目标、技术方案、个人职责、评测指标、上线结果、问题复盘
**适用场景：** 项目复盘、项目真实性、失败经验与个人贡献追问
**回答：** 我会按“业务目标—技术方案—个人职责—评测指标—上线结果—问题复盘—下一步计划”展开。先说明为什么做和成功标准，再解释为什么选择模型、RAG、Agent、Workflow 或 Tool；然后明确我个人的决策与交付，用可验证指标说明阶段结果，最后讲失败、降级和下一步，而不是只罗列技术名词。

**背景：** 面试官通常不仅判断方案是否先进，还会核实项目是否真实、候选人做了什么、怎样证明有效，以及系统失败时能否负责。
**具体做法：**
1. 业务目标：交代用户、场景、原流程痛点、基线和期望业务指标。
2. 技术方案：画清数据、知识、模型/Agent、工具和应用链路，说明选型与不用其他方案的原因。
3. 个人职责：区分我主导的产品决策、我参与的协作工作和团队负责的算法/工程实现。
4. 评测与结果：分别说明离线质量、线上稳定性和业务价值，并给出数据来源、时间范围与计算口径。
5. 复盘与计划：选择一个 Bad Case，说明如何定位、修复、回归和防止复发，再提出下一步假设和验证方式。

**结果：** 这套框架能把项目讲成可核验的决策闭环。若项目仍是方案、开发或试点，我会如实说明阶段；没有样本量、口径或业务证据的数字一律不作为已验证成果。

**追问补充：**
- 面试前为每个项目准备一张架构图、一组指标口径、一个 Bad Case 和一条职责边界。
- 如果结果不理想，我会说明得到的证据、已经采取的动作和下一轮验证计划，不把预期结果说成事实。
